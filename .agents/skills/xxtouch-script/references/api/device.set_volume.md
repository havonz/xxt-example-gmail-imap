# device.set_volume

Purpose: Set volume

## Signature
```lua
device.set_volume(volume)
```

## Example
```lua
device.set_volume(0) -- Mute the device.
```

## Parameters
- volume
    number, the target device volume, in the range `0.0` to `1.0`.
