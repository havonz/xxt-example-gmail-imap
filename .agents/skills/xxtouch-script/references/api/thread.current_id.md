# thread.current_id

Purpose: Current task ID

## Signature
```lua
task_id = thread.current_id()
```

## Example
```lua
nLog('Current task ID', thread.current_id())
```

## Returns
- task_id
    integer, the current task ID.
